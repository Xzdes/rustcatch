name: Continuous Integration

on:
  push:
    branches: [ main, master ] # Запускать на коммиты в main или master
  pull_request:
    branches: [ main, master ] # Запускать на Pull Request в main или master

jobs:
  test-build:
    name: Test Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxdo-dev libxtst-dev

      - name: Install Node.js dependencies
        run: npm install --ignore-scripts

      - name: Build native addon to test compilation
        run: npm run build